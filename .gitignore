# Ignore Visual Studio temporary files, build results, and
# files generated by popular Visual Studio add-ons.

# User-specific files
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# User-specific files (MonoDevelop/Xamarin Studio)
*.userprefs

# Mono Auto Generated Files
mono_crash.*

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Uncomment if you have tasks that create the project's static files in wwwroot
# wwwroot/

# Uncomment if you have tasks that create a dist folder
# [Dd]ist/

# Node modules folder, should match the value of nodeModulesPath in .yarnrc
node_modules/

# Mypy cache
.mypy_cache/

# Coverage directory used by tools like istanbul
coverage/

# Publish directory
[Pp]ublish/
# when using JetBrains Rider
.idea/
.idea/*
# Rider does not create the `workspace.xml` file until one of its known project file types was opened.
# Thus, if you use Rider, uncomment this.
# .idea/workspace.xml

# ! is used to negate exclusions, meaning the specified file(s)/directory(ies) should not be excluded when a parent directory is excluded
!.gitattributes
!.gitignore

# Rider does not create the `workspace.xml` file until one of its
# known project file types was opened. Thus, if you use Rider, uncomment this.
# .idea/workspace.xml

.vscode/
.vscode/*
# Visual Studio Code metadata files, should match the value of `files.exclude` entry
# Build folders may be defined in Visual Studio Code via settings like `files.watcherExclude`

# Directory for IDE specific project files when building on Windows
# This can also be modified using `dotnet build -o buildpath %sources path%`
.vscode
.idea
.vs
